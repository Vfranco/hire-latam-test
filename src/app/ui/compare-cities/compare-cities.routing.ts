import { Routes } from '@angular/router';
import { CompareCitiesViewComponent } from './view/compare-cities-view.component';

export const compareCitiesRouting: Routes = [
  { path: '', component: CompareCitiesViewComponent }
]
